# RESEARCH — sailvlog v3（レースリプレイ・デバッガ E2 本線）

<!-- 契約: 作成者 architect（RQ起票と統合判断の2段階） / 入力: PRD.md rev.3 + docs/dev-org/research/*.md / 出力先: ARCH.md, TASKS.md（Architecture Gate の判定材料） -->
<!-- 運用: architect 1回目起動でセクション1+スパイク計画まで作成（2026-07-24） → Team Lead が researcher を観点別に並列起動 → architect 2回目起動で research/*.md を読みセクション2以降を完成 -->
<!-- researcher の生メモの形式は templates/RESEARCH-NOTE.md が唯一の正 -->

## 0. 前提確認 — v2原本とPRDの整合（architect実施・2026-07-24）

PRD rev.3 §0 が「本環境でgit show不可のため未読」としていたv2原本（`origin/v2/glossary-page:docs/v2/00〜05`）を全6ファイル読了した。結論: **PRDの前提と実質的な矛盾はない**。PRDの要約（ペルソナ一人=タクミ／辞典形式／SNS全凍結／「実践知こそ価値の核」）は原本どおりで、E2の注釈機能はv2のL3思想（固定の答えでなく判断の軸を残す=「なぜここでタック?」）の直系にあたる。留意点は2つ。①v2の04-inventoryは「削除はしない・凍結で運用」の方針だったが、PRD v3はPost/PostLike/Followを削除候補としており方針が変わっている（PRDが上位。ARCH.mdで凍結/削除の扱いをADR化する）。②v2原則3「重い・複雑なUIは原則違反・スマホ30秒」はリプレイエンジンと表面上衝突するが、これは辞典（読む=通学電車）前提の原則であり、E2の主利用文脈は反省会（プロジェクタ/PC）とPRD§3で再定義済みのため、ピボットによる前提変更であって矛盾ではない。ただし「スマホでも閲覧」はPRD要件に残っているので、再生エンジンのモバイル性能はスパイクの検証対象に含める（後述）。

## 1. Research Questions（設計判断リスト）

<!-- 判断に紐づかないRQは書かない。候補が1つしかない問いはRQではなく前提としてARCHに書く -->

凡例:
- **基盤タグ**: 〔共通〕= A主役に転んでも使う判断（無駄にならない）/〔E2〕= E2主役確定後にのみ効く判断
- **優先度**: **②必須** = Architecture Gate判定までに decided が必要 /「実装中可」= 方向だけ決めて詳細は実装フェーズで確定してよい
- 新技術の学習コスト予算は**プロジェクトあたり1つ**（現状フロントはNext.js+React素のみ。地図/描画ライブラリを入れるならそれが唯一の新技術枠。RQ-01/02/05の候補選定はこの予算に従属する）

| ID | 判断の問い（択一形式） | 候補(2〜4) | 基盤タグ | 優先度 | 担当観点 | status |
|---|---|---|---|---|---|---|
| RQ-01 | 2D再生エンジンの描画方式はどれか（6艇×2h×1Hzを60fpsで再生・スクラブできる方式） | (a) Canvas 2D直描き・自前実装（地図なし or 静的背景） (b) MapLibre GL JS等の地図ライブラリ上にカスタムレイヤ/マーカーで重畳 (c) deck.gl等WebGL可視化ライブラリ (d) SVG+DOM要素をCSS transformで動かす | 〔E2〕 | **②必須**（スパイク対象） | 再生エンジン | open |
| RQ-02 | 背景は地図タイルを出すか、無地海面＋スケールバーか | (a) OSM系ラスタ/ベクトルタイル（MapLibre+無料タイルソース。タイル利用ポリシー・商用可否・オフライン可否が論点） (b) 無地Canvas＋メルカトル→ローカル平面投影＋スケールバー（反省会の議論には相対位置で足りる仮説） (c) 静的タイル画像を1回だけ取得しキャッシュ | 〔E2〕 | **②必須**（RQ-01と連動、スパイク構成に影響） | 再生エンジン | open |
| RQ-03 | 複数艇の時刻同期・補間はどの方式か | (a) 取込時に共通1Hzグリッドへ事前リサンプリング（線形補間・欠測はギャップ扱い）し、再生時は配列indexアクセスのみ (b) 生タイムスタンプを保持し再生時に二分探索＋線形補間 (c) スプライン等の平滑補間で見た目優先 | 〔E2〕 | **②必須**（スパイク対象・保存形式RQ-06と連動） | 再生エンジン / データ処理 | open |
| RQ-04 | 再生状態（現在時刻・速度・選択艇）の管理方式はどれか | (a) React stateの外に再生クロック（requestAnimationFrame＋ref）を置き、描画は命令的に更新。UIには低頻度(≦10Hz)でのみ同期 (b) Zustand等の軽量ストアで全部管理（新技術枠を消費） (c) useState/Contextのみ（60fpsでのre-render懸念） | 〔E2〕 | **②必須**（60fps要件の成否に直結） | フロント設計 | open |
| RQ-05 | 航跡データの保存形式はどれか | (a) 原本GPXをファイル/オブジェクトとして保存し、正規化済みトラック（列指向のtimestamp/lat/lon配列）をJSONB等で1艇1レコード保存。DBに点を行展開しない (b) TrackPointを1fix=1行でDB化（1練習=約4.3万行。時刻範囲クエリは容易だが取得コスト大） (c) 原本GPXのみ保存し毎回フロントでパース | 〔E2〕 | **②必須**（Prismaスキーマ設計に直結） | データモデル | open |
| RQ-06 | GPXパースは自前かライブラリか | (a) DOMParserで自前（GPX 1.1のtrkpt/time/ele/extensionsだけ読む。依存ゼロ） (b) 既存パーサライブラリ（メンテ状況・1Hz大容量での性能が論点） | 〔E2〕 | 実装中可 | データ処理 | open |
| RQ-07 | 既存Express/Prisma基盤への載せ方はどれか | (a) 既存モノリスに新モデル（Session/Track/Annotation等）を追加し、凍結機能はルーティング無効化で共存 (b) v3用に新規アプリを別立てし、User/Team認証だけ既存DBを共有 (c) 凍結対象モデル・ルートを物理削除してから同一アプリに追加 | 〔共通〕（A主役でもカード用モデル追加で同じ判断） | **②必須** | 既存資産統合 | open |
| RQ-08 | MVPのホスティングはどれか（反省会でURL共有するには公開URLが必要） | (a) 現行docker-compose構成のまま無料/低額PaaS（Render・Fly.io等）へ (b) フロントVercel＋DBはNeon/Supabase等マネージドPostgres（Expressの置き場が論点） (c) MVP期間はローカル＋反省会場でLAN共有（公開しない） | 〔共通〕 | **②必須**（RQ-07の構成判断を拘束する） | インフラ | open |
| RQ-09 | タイムライン注釈のアンカー設計はどれか | (a) セッション相対時刻のみに紐づく単一Annotationモデル（対象艇・位置は持たない。最小） (b) 時刻＋対象艇(任意)＋レグ(任意)を持つ（「この艇のこのタック」を特定できるが入力が増える） (c) v1 Commentモデルを拡張流用 | 〔E2〕 | 実装中可（②では方向のみ） | データモデル | open |
| RQ-10 | O2コースのレグ頭出し（手動指定）のデータ設計はどれか | (a) マーク座標をユーザーが地図上で指定→各艇のレグ境界（マーク回航時刻）を最近傍通過で自動算出 (b) セッション共通のレグ境界「時刻」を手動指定（艇差は無視。最小） (c) 艇ごとにレグ境界時刻を手動指定（正確だが入力コスト最大） | 〔E2〕 | 実装中可 | データモデル | open |
| RQ-11 | モバイル収録（マネ艇・操作ほぼゼロ）の現実解はどれか | (a) 既存無料GPSロガーアプリをハンドブックで指定（1Hz固定・バックグラウンド2h録画・GPX出力可・iOS/Android両対応が選定条件。候補アプリの実名比較が調査対象） (b) 自前PWA/簡易収録ページ（Geolocation APIのバックグラウンド制約が論点） (c) 安価GPSロガーハード購入（部予算。精度は最良だが台数×費用） | 〔E2〕 | **②必須**（収録ハンドブック＝MVPスコープの一部。並行検証E2の手順にもそのまま使う） | 収録・運用 | open |
| RQ-12 | 注釈込みURL共有の状態の持ち方はどれか | (a) URLクエリ（?t=…&boats=…）で再生位置・選択艇を運び、注釈本体はDB (b) 共有スナップショットをDB保存しshort URL発行 | 〔E2〕 | 実装中可 | フロント設計 | open |

**RQにしなかった前提（候補が実質1つのもの）**: 認証・Team/TeamMember・BoatType・デザイントークンのv1流用（PRD§5で確定済み）／GPXを入力形式の第一級とすること（PRD確定。KML対応はTracTracイベントが取れた場合の変換で足りるため将来判断）。

**A主役に転んだ場合に無効になるRQ**: RQ-01〜06, 09〜12（E2タグ全部）。**生き残るRQ**: RQ-07・RQ-08（カード図鑑でも「既存基盤への新モデル追加方法」と「公開URL」は同一の判断）。E2タグのRQ調査はスパイク（下記）と②Gate判定に必要な範囲に絞り、A転向リスクが解消する並行検証（〜2週間）と並走させる。

---

## スパイク計画 — SPIKE-01: GPX複数艇リプレイの性能検証

<!-- GATES.md ①通過記録「E2の技術検証スパイクは先行してよい」に基づく。PRD§7の最大技術リスク（供給リスクは並行検証が担当）のうち、本スパイクは第2の技術リスク「1Hz・複数艇・2時間分の快適再生」を潰す -->

### 目的（潰す仮定）

「スマホGPXの1Hzデータ・6艇・2時間分を、ブラウザ（PC＋オーナーのスマホ）で快適にスクラブ再生できる」— これが崩れるとE2のMVPスコープ（PRD§5の2Dスクラブ再生）全体が崩れる、本設計で一番危ない技術仮定。あわせてRQ-01（描画方式）とRQ-03（補間方式）の判定材料を実測で得る。

### PASS基準（数値定義）

対象データ: **6艇 × 2時間 × 1Hz = 43,200点**（7,200点/艇）。計測環境: 開発機ブラウザ（Chrome）＋オーナーのスマホ実機（反省会のプロジェクタ投影はPC経由なのでPC基準が本線、スマホは閲覧要件）。

| # | 項目 | PASS基準（PC） | PASS基準（スマホ実機） |
|---|---|---|---|
| 1 | 通常再生（1x〜8x倍速）のフレームレート | 平均55fps以上かつ1フレーム描画16ms以内 | 平均30fps以上 |
| 2 | シーク（タイムラインの任意位置へジャンプ） | 表示更新まで1秒以内（目標200ms） | 1秒以内 |
| 3 | 初期ロード（GPX6本のパース＋正規化＋初回描画） | 3秒以内 | 5秒以内 |
| 4 | メモリ | 再生30分相当の連続スクラブでヒープ増加が発散しない（リークなし） | 同左・クラッシュしない |
| 5 | 航跡テール表示（各艇の過去N分の軌跡を残す描画）ON時 | 上記1〜2を維持 | 同左 |

判定: **全項目PASS → RQ-01/03の当該方式を採用しARCH.mdへ**。一部FAIL → 縮退策（下記）適用後に再計測。縮退込みでもFAIL → 描画方式を次候補へ切替（それでもFAILならTeam Lead経由でPRDの「快適再生」要件の緩和を協議）。

### 検証内容（実施は次工程のスパイク担当。ここは手順定義のみ）

1. **合成GPX生成スクリプト**（使い捨て・Node or Python）
   - 470のO2コースを模したジグザグ航跡（上り: タック往復、下り: ジャイブ）を6艇分、艇ごとに速度・タックタイミングを微妙にずらして生成。1Hz・2時間・GPX 1.1形式
   - **GPSノイズモデル**: 各点に σ=3m と σ=8m の2水準のガウスノイズを付加した版も生成（スマホGPS誤差の想定範囲）。→ 生ノイズ表示とスムージング（移動平均等）後の表示を目視比較し、「タックの議論に耐える見た目か」のスクリーンショットを記録（精度の実地検証は並行検証E2の担当。ここでは表示側の耐性のみ確認）
   - 欠測ギャップ（30秒×2箇所）を1艇に入れ、補間・描画が破綻しないことを確認
2. **再生プロトタイプ2本**（同一データ・同一UI骨格で比較。使い捨てコード、`spike/` ディレクトリに隔離し本実装に混ぜない）
   - 案(a): Canvas 2D直描き（無地背景＋ローカル平面投影＋スケールバー）— RQ-01(a)×RQ-02(b)
   - 案(b): MapLibre GL JS＋カスタムレイヤ — RQ-01(b)×RQ-02(a)
   - 共通機能: 再生/停止・1x/4x/8x・シークバー・航跡テール・2艇ハイライト。補間はまずRQ-03(a)（事前1Hzグリッド化＋indexアクセス）で実装し、十分ならそれで確定
3. **計測**: rAFコールバック間隔のログ（p50/p95）、`performance.now()` によるシーク応答、Chrome DevTools Performance/Memoryプロファイル。スマホは実機で同ページを開き体感＋簡易fpsログ
4. **記録**: 結果を本ファイル §4（再調査ログ）と RQ-01/02/03 の判定根拠として記入。スクリーンショット/数値を `docs/dev-org/research/spike-replay.md`（researcher形式）または本ファイル添付で残す

### FAIL時の縮退策（適用順）

1. 描画点の間引き（画面ズームレベルに応じたLOD。1Hz全点描画→表示は2〜5秒間隔点）
2. 航跡テールを Path2D 事前構築＋差分描画に変更（毎フレーム全線分再描画をやめる）
3. テール長の上限（直近10分など）
4. OffscreenCanvas/WebGL化（RQ-01候補(c)へ移行。ここまで来たら新技術枠の再配分をTeam Leadと協議）

### 所要目安

- 合成GPX生成: 0.5日
- プロトタイプ2本＋計測: 1日
- **合計1.5日（上限2日）**。2日で終わらない場合は案(a) Canvas直描きのみに絞って完了させる（比較は不完全でも「成立するか」の答えを優先）

---

## 2. 情報源台帳（統合ビュー）

<!-- architect 2回目起動（2026-07-24）で記入。生の全行は research/*.md 参照。ここは統合時に効いた主要ソースのみ -->

| # | 入力 | 担当RQ | ソース数 | 一致状況 | 統合時の留保 |
|---|---|---|---|---|---|
| 1 | `research/replay-engine.md`（researcher①） | RQ-01,02,04,12 | 12 | 全RQで独立2ソース以上一致 | RQ-12は「軽く」指定で反証探索が浅い（確証度低め）と自己申告あり。決定は業界慣行との同型性＋実装可逆性で補う |
| 2 | `research/data-model.md`（researcher②） | RQ-03,05,06,09,10 | 16 | RQ-03は方式が分岐（→SPIKE-01で決着）。RQ-05はTOAST一般論一致だが「本用途への当てはめ」は未検証。RQ-10は一次ソース実質1件 | RQ-05に実装中の実測ガードを置く（§3.2）。RQ-10は「実装中可」優先度なので方向決定＋詳細保留が許容範囲 |
| 3 | `research/infra-recording.md`（researcher③） | RQ-07,08,11 | 13 | 全RQで一致（RQ-08は5社公式が「0円恒久運用は不可」で一致） | RQ-08の「VercelにExpressをどう載せるか」が未解決 → 本統合でarchitectが補完調査し決着（§3.6①） |
| 4 | `research/spike-replay.md`（SPIKE-01実測） | RQ-01,03（＋02,04の傍証） | 実測1式 | PC全項目PASS（fps60 / render p95 0.2ms / シーク0.3ms / メモリ発散なし / テールON影響なし） | **スマホ実機は未計測（オーナー待ち）**。MapLibre比較(案b)は未実施 — Team Lead受容済み（文献2ソースが構造的不利を裏付けるため） |
| 5 | architect補完（2026-07-24、本統合時） | RQ-08残論点 | 2 | [Vercel公式 Express on Vercel](https://vercel.com/docs/frameworks/backend/express)＝Expressはserverless関数化が前提（単一バンドル250MB上限・実行時間上限・Expressのエラーページがfunction状態を汚す注意あり）。[Vercel Functions Limits](https://vercel.com/docs/functions/limitations)＝常駐プロセスの実行モデルではない | Express本体をVercelに載せる案は「動かない」ではなく「serverless化の改修コスト＋制約を払う」案。今回は改修ゼロのRender別置きを選ぶ（§3.4 RQ-08） |

## 3. 統合判断

### 3.1 複数ソースで共通する設計（独立2ソース以上）

1. **60fps再生はReact stateの外で行う（rAF＋ref命令的更新、UIへは低頻度同期のみ）** — Zustand公式のTransient Updatesパターン／dev.to記事群（別著者）／React公式「canvas内部はReact管理外として直接操作可」が独立一致。SPIKE-01がこの設計で60fps実測を出し実証済み
2. **地図FWのカスタムレイヤは毎フレーム地図全体を再描画する構造的コストを持つ** — Mapbox GL JS Issue #7629/#8159（60fps→15fps・CPU4%→40%の一次報告）＋dev.to（Canvas直描きの原理説明）。SPIKE-01のCanvas直描き全PASSと併せてRQ-01の決定根拠（Team Lead申し送り1: スパイク実測＋文献2ソースの併記）
3. **2KB超のJSONB値はTOAST化され、読み取り集計・頻繁更新パターンでは2〜10倍劣化しうる** — PostgreSQL公式TOAST文書／evanjones実測ブログ／ScaleGridブログの独立3ソース。ただし劣化条件は「大量行スキャン・頻繁更新」であり、本用途（write-once・主キー1件フェッチ）とは前提が異なる（§3.2）
4. **破壊的スキーマ変更はexpand→contractの2段階で行う** — Prisma公式データ移行ガイド＋実務ブログが独立一致。凍結モデルの扱い（ADR-003/005）の根拠
5. **無料ホスティングに「0円・無制限・恒久」は存在しない** — Render/Fly.io/Railway/Neon/Supabaseの5社公式が独立一致。0円構成は「スリープ・容量上限を仕様として受け入れる組み合わせ」でのみ成立
6. **PWA/WebのバックグラウンドGPS長時間収録は不成立** — WebKit公式ブログ（タブサスペンド）＋業界解説記事群（Geofencing API提案放棄）。RQ-11(b)自前PWA案の棄却根拠
7. **時刻アンカー必須・対象（艇/レグ）は任意フィールド同居が注釈設計の慣行** — 動画注釈OSS（一次確認済み）＋スポーツ映像分析ツール群（二次・弱）。弱い方は主根拠にしない
8. **「再生位置はURLクエリ・本体データはDB」が時間軸メディア共有の業界標準** — YouTubeの`?t=`慣行＋nuqs等URL状態管理OSSの設計思想

### 3.2 意見が分かれる部分と、このプロジェクトでの判定理由

- **RQ-03: 事前グリッド化 vs 生タイムスタンプ＋実行時補間** — 実運用OSSで採用が割れた（LeafletPlayback=事前グリッド／deck.gl TripsLayer=実行時GPU補間）。文献では決着不能のため**SPIKE-01の実測で決着**: 事前1Hzグリッド化＋indexアクセスで全PASS・実装も単純。生タイムスタンプ方式の利点（情報量無損失）は、原本GPXを別途保存することで担保する（グリッドは表示用キャッシュと位置づけ、再生成可能）
- **RQ-05: 列指向JSONB vs 1fix=1行展開** — 反証ソース（正規化行推奨）の前提は「時刻範囲集計・複数艇横断クエリを頻繁に行う」用途。本プロジェクトの主用途は「1練習分をまるごと読んで再生」のwrite-once/read-manyで前提が異なる → JSONB採用。ただし文献は本用途での実測を提供していないため、**実装中ガード**を置く: ①「JSONB内部をWHEREで検索するクエリは書かない」を設計上の約束としてARCH.mdに明記 ②実データ取込後にセッションGET応答を1回実測し、p95>1sならbytea圧縮/レコード分割へ縮退（TASKS.md T-24）
- **新技術予算の使い先** — deck.gl（性能面では十分だが43,200点にWebGLは過剰）・Zustand（高頻度更新では公式自身がref+subscribeに帰着させている）・nuqs（素のuseSearchParamsで代替可）はいずれも「導入理由が消えた」。**新技術枠は温存**し、スマホ実機FAIL時のWebGL化（OffscreenCanvas等）に備える

### 3.3 スコープ判定: 今回作る / 将来作る / 作らない

| 項目 | 分類 | 理由 | 根拠(RQ-XX) |
|---|---|---|---|
| Canvas 2D直描き再生エンジン（無地海面＋スケールバー） | 今回 | SPIKE-01全PASS・依存ゼロ・PRDコア | RQ-01,02,03,04 |
| GPX取込（フロントDOMParserパース＋プレビュー＋サーバ再検証） | 今回 | PRDコア。依存ゼロで実装小 | RQ-06 |
| Session/Track/Annotationモデル＋列指向JSONB保存＋原本GPX保存 | 今回 | PRDコア。1艇1レコードで行展開しない | RQ-05,09 |
| タイムライン注釈（時刻必須・艇/レグ任意） | 今回 | PRDコア | RQ-09 |
| レグ頭出し（マーク手動指定→セッション共通境界時刻算出＋手動補正） | 今回 | PRD明記。ChartedSailsの「自動算出＋手動修正」2段構えを踏襲 | RQ-10 |
| 部内共有（Team流用＋URLクエリ`?t=&boats=`） | 今回 | PRDコア | RQ-12 |
| 収録ハンドブック（Geo Tracker指定） | 今回 | PRDでプロダクトの一部と定義 | RQ-11 |
| 凍結ルート無効化＋Vercel/Render/Neonデプロイ | 今回 | 〔共通〕A主役でも必要 | RQ-07,08 |
| 地図タイル/静的衛星写真背景 | 将来 | 無地で反省会議論は足りる仮説をMVPで検証。タイル利用はUser-Agent/ロゴ/遮断リスクの運用コストが付随 | RQ-02 |
| 表示用スムージング（移動平均） | 将来 | σ=8mノイズ時のタック頂点議論に必要になる可能性（SPIKE-01所見）。並行検証の実データを見てから | RQ-01(スパイク所見) |
| マーク回航の自動検出・艇ごとレグ境界 | 将来 | PRDが「自動検出は将来」と明記。手動＋補正で開始 | RQ-10 |
| Post/PostLike/Followの物理削除（contract） | 将来（バックログBL-01。MVP計画外） | expand→contract原則。実施条件=MVP実運用安定後＋オーナー明示承認（Codexレビュー裁定でMVP必須から降格） | RQ-07 |
| MapLibre/deck.gl/Zustand/nuqs導入 | 作らない | 導入理由が消滅（§3.2）。新技術枠は温存 | RQ-01,04,12 |
| 自前PWA収録・GPSロガーハード購入 | 作らない | PWAはバックグラウンド収録不成立／専用機$445は「安価」前提に反する | RQ-11 |
| TrackPointの1fix=1行展開 | 作らない | 集計クエリ前提が本用途にない。1練習4.3万行の挿入/取得コストだけ払うことになる | RQ-05 |
| 共有スナップショットDB保存＋short URL | 作らない | URLクエリで足りる。リソース増殖の管理コスト回避 | RQ-12 |
| JSONB内部へのWHEREクエリ | 作らない（設計上の約束） | JSONBは統計を持たずプランナー最適化不能。丸ごと取得のみに限定 | RQ-05 |

### 3.4 採用技術・設計の決定表

| RQ | 決定 | 根拠ソース(最低2・独立) | 却下候補と理由 | ARCH.md反映先 | status |
|---|---|---|---|---|---|
| RQ-01 | **(a) Canvas 2D直描き** | SPIKE-01実測（全PASS・render p95 0.2ms）＋Mapbox Issue #7629/#8159（カスタムレイヤの全体再描画コスト）＋dev.to（Canvas+rAFの原理） | (b)MapLibre重畳=構造的再描画コスト＋新技術枠消費。(c)deck.gl=43,200点にWebGLは過剰・重量級。(d)SVG+DOM=6艇×テールのDOM更新コスト | §1表, ADR-001 | **decided** |
| RQ-02 | **(b) 無地海面＋ローカル平面投影＋スケールバー** | OSM公式タイルポリシー（UA必須・遮断リスク）＋MapTiler公式（非商用限定・ロゴ必須）— 両者とも「無条件フリーランチではない」で一致。SPIKE-01スクリーンショットで無地背景でもタック議論可能を確認 | (a)タイル=可用性・規約リスクを反省会当日に持ち込む。(c)静的画像キャッシュ=将来の拡張候補として温存（researcher①の新RQ候補を将来判断に格下げ） | §1表, ADR-001 | **decided** |
| RQ-03 | **(a) 取込時に共通1Hzグリッドへ事前リサンプリング＋再生時indexアクセス** | SPIKE-01実測（シークp95 0.3ms）＋LeafletPlayback実装（同方式の実運用例） | (b)生タイムスタンプ+二分探索=性能上の必要なし・実装複雑。(c)スプライン=見た目優先で議論を誤誘導するリスク。情報量無損失は原本GPX保存で担保 | §3データモデル, ADR-002 | **decided** |
| RQ-04 | **(a) rAF＋refの再生クロック、Canvasへ命令的描画、UIへは≦10Hz同期** | Zustand公式Transient Updates（高頻度更新はref+subscribeに帰着）＋dev.to記事群＋React公式（canvas内部は安全な直接操作範囲） | (b)Zustand=導入しても同じパターンに行き着き新技術枠だけ消費。(c)useState/Contextのみ=60fps re-renderコスト | §2構成, ADR-001 | **decided** |
| RQ-05 | **(a) 原本GPXテキスト＋列指向JSONB（1艇1レコード）** | PostgreSQL公式TOAST＋evanjones実測＋ScaleGrid（TOAST一般論3ソース一致）。反証（正規化行推奨）は集計クエリ前提で本用途と不一致 | (b)1fix=1行=1練習4.3万行の展開コストに見合う集計需要がない。(c)原本のみ毎回パース=毎回のパースコスト＋時刻同期の再計算 | §3データモデル, ADR-002 | **decided**（実装中ガードT-24付き） |
| RQ-06 | **(a) DOMParserで自前パース（フロントで実施・サーバ再検証）** | GPX 1.1公式仕様（必須要素はtrkpt@lat/lon＋timeのみとシンプル）＋OSSパーサ2件の実装範囲確認（同範囲を軽量処理） | (b)ライブラリ=gpxparserは5年未更新、gpxjsは★34と実績薄。依存を増やす理由がない。gpxjsのベンチ(10k点162ms)は「自前でも軽い」ことの傍証に使う | §4 API, ADR-006 | **decided** |
| RQ-07 | **(a) 既存モノリスに新モデル追加＋凍結はルート無効化（削除はcontractで後段）** | Prisma公式expand&contract＋実務ブログ（破壊的変更の段階分けで一致） | (b)アプリ別立て=認証/Team共有の二重管理コスト。規模に対し過剰。(c)物理削除先行=一括破壊はexpand&contract原則に反する | §2構成, ADR-003/005 | **decided** |
| RQ-08 | **(b)改: フロント=Vercel、Express=RenderのDocker Web Service（無料）、DB=Neon無料枠** — 全構成0円 | Render公式（無料Web Serviceはspin-downのみで失効なし。失効するのは無料Postgres）＋Neon公式（0.5GB・失効なし）＋Vercel公式Express文書（serverless化前提＝常駐Expressの置き場にならない） | (a)Render一式=無料Postgresが30日失効で0円不成立。Fly.io/Railway=無料枠実質なし。(c)LAN共有=部内の別日閲覧・URL共有というPRD要件を満たさない | §2構成, ADR-004 | **decided** |
| RQ-09 | **(b)方向: 時刻必須＋対象艇(任意)＋レグindex(任意)の単一Annotationモデル** | 動画注釈OSS（時刻必須・対象任意の同居設計を一次確認）＋スポーツ映像分析ツール慣行（二次・傍証のみ）。入力コスト懸念には「時刻は再生位置から自動キャプチャ・艇/レグはタップ任意」のUXで対応 | (a)時刻のみ=「この艇のこのタック」が特定できず注釈の資産価値が下がる。(c)v1 Comment拡張=Article前提の外部キーを引きずる | §3データモデル | **decided(方向)**・フィールド詳細は実装中可 |
| RQ-10 | **(a)簡易版: マーク座標を地図上で手動指定→基準艇の最近傍通過からセッション共通レグ境界時刻を自動算出＋手動補正UI** | ChartedSails公式解説（マーク指定＋ドラッグ補正の2段構えが実運用パターン。一次確認済み）。二次ソース(SailFrames)は裏取り失敗のため不使用 → **一次1件のため「方向decided・詳細保留」**（優先度=実装中可の範囲内） | (b)共通時刻の手動指定のみ=PRD明記の「マーク位置の手動指定」から外れる（スコープ改変になるため不採用。ただしFAIL時の縮退先として温存）。(c)艇ごと手動=入力コスト最大 | §3データモデル | **decided(方向)・詳細保留** — 保留理由: 独立2ソース基準未達（一次確認はChartedSails 1件のみ、2件目は裏取り失敗）。追加調査はTeam Lead裁定（2026-07-24）で不実施 — 優先度「実装中可」のRQにつき方向決定で②Gate判定材料としては足りると受容（受容記録はGATES.md）。境界算出アルゴリズム・補正UIの詳細はT-21実装時に確定し、実装で仮定が崩れたら§4再調査ログ経由で再開する |
| RQ-11 | **(a) 既存GPSロガーアプリ指定（Geo Tracker・記録間隔最短設定）** | VALIDATION.md §2-1（一次ソース参照済みの社内調査）＋WebKit公式ブログ＋業界記事群（(b)PWA不成立の裏付け） | (b)自前PWA=バックグラウンドJS停止で2h収録不成立。(c)専用ハード=SpeedPuck $445で「安価」前提が崩壊。部員所有デバイス転用は運用中の機会的確認へ（§3.6③） | §5運用, 収録ハンドブック | **decided** |
| RQ-12 | **(a) URLクエリ（`?t=&boats=&leg=`）＋注釈本体はDB** | YouTube `?t=`慣行（複数メディア確認）＋nuqs OSS設計思想。60fps時刻をURLへ直結せず「一時停止/シーク確定時のみreplaceState」で履歴汚染を回避 | (b)スナップショットDB+short URL=リソース増殖・管理コストに見合う要件がない | §4 API | **decided** |

### 3.5 実装順序への示唆（TASKS.md の実装スライスS0〜S3構成への入力。※「Phase」はPRDのプロダクト展開フェーズ専用語とし、実装単位には使わない）

1. **〔共通〕タスクを先頭に**: 凍結ルート無効化・0円ホスティング疎通は主役検証（GATES ①）の結果に関係なく必要 → 検証待ち期間に消化できる
2. **縦貫通の1本目**は「合成GPX 1本を取込→1艇再生」ではなく、最初から**複数艇**で通す（時刻同期＝startSecオフセット合わせがこのプロダクトの本質的難所で、1艇では検証にならない）
3. 再生エンジンはSPIKE-01の設計（グリッド＋indexアクセス＋rAF/ref）を**参照して本実装として書き直す**（spike/はコピー元でなく参考。使い捨て契約を守る）
4. レグ頭出し・2艇比較はコア貫通後のS2（削る順序の先頭でもある）
5. スマホ実機計測（B-2）はS1完了判定までに必須。FAILでもPC/プロジェクタ本線は崩れないが、縮退策の適用工数をS2に積む

### 3.6 新RQ候補3件の裁定（Team Lead申し送り3）

| # | 候補 | 裁定 | 理由 |
|---|---|---|---|
| ① | ExpressをVercel構成でどう動かすか（researcher③） | **RQ化せず本統合で決着** | architectが最小限補完（§2台帳#5）。Vercel公式はExpressをserverless関数化して載せる方式のみ提供し、改修コスト＋実行時間/バンドル制約＋エラーハンドリング注意が付随。改修ゼロで済むRender無料Web Service別置きが存在する以上、比較は自明でRQに値しない → RQ-08の決定に吸収 |
| ② | JSONB保存の実測をSPIKE-01に追加（researcher②） | **実装中に確認（T-24）** | write-once/read-many・主キー1件フェッチという用途はTOAST劣化の主条件（集計スキャン・頻繁更新）に該当せず、事前リスクは低い。FAIL時の縮退（bytea圧縮/レコード分割）も局所的な変更で済む。専用スパイクを立てるROEが合わない — 実装後に本物のコードパスで1回計測する方が証拠の質も高い |
| ③ | Garmin/Apple Watch/GoProのGPXエクスポート実機検証（researcher③） | **やらない（今は）** | RQ-11はGeo Trackerでdecided済みで、設計判断（データモデル・API）はどの収録手段でも「GPX 1.1が来る」で不変。デバイス転用は収録ハンドブックに「1Hz GPXが出せる手段なら何でも可」と書き、並行検証〜運用中に部員の実機で機会的に確認する運用事項。実装をブロックしない |

## 4. 再調査ログ（実装後ループ）

| 日付 | 契機(Gate FAIL/UX監査/実装で判明/スパイク結果) | 追記・再開したRQ | 再判断結果 | ARCH.md改訂 |
|---|---|---|---|---|
| 2026-07-24 | スパイク結果(SPIKE-01) | RQ-01, RQ-02, RQ-03 | 案(a) Canvas 2D直描き＋RQ-03(a)事前1Hzグリッド化+indexアクセスで全PASS基準(fps/シーク/初期ロード/メモリ/テールON)を余裕で達成（6艇x2時間x1Hz=43,200点、renderFrame実行時間p95=0.2ms、シーク応答p95=0.3ms）。縮退策は不要。案(b) MapLibreは未実施(案aで十分なため優先度低)。σ=8mノイズ下ではタック頂点の精密位置特定にスムージングが要検討(将来課題)。スマホ実機は未計測(オーナー実施待ち、手順は spike/README.md に記載)。詳細: `docs/dev-org/research/spike-replay.md` | RQ-01→(a)採用、RQ-03→(a)採用でARCH.mdへ反映（次工程） |
| 2026-07-24 | 統合フェーズ（architect 2回目起動） | RQ-08（Vercel×Express残論点） | Vercel公式Express文書＋Functions Limitsを補完確認 → Express本体はRender無料Web Serviceへ別置き（§3.6①） | ADR-004として新規記載 |
| 2026-07-24 | Codexセカンドオピニオンレビュー（②Gate判定前） | （RQ再開なし） | 15指摘に対応（契約間矛盾の同期・実装単位を「実装スライスS0〜S3」へ改名・T-01の可逆限定・T-93→BL-01バックログ降格・B-1サブゲート化ほか）。RQ-10の独立2ソース未達はTeam Lead裁定で追加調査せず受容（GATES.mdに記録） | ARCH.md/TASKS.md/PRD.md改訂（コミット参照） |

## 5. knowledge 還流候補

還流はリリース工程（release-manager / `/til`）で実施。候補:

- **til候補**: ①「地図FWカスタムレイヤは`triggerRepaint()`で毎フレーム地図全体を再描画する」構造知見（Mapbox #7629: 60fps→15fps）②「Zustandの高頻度更新は公式自身がref+subscribe（Transient Updates）に帰着させている＝60fps用途でストア導入の理由にならない」③「iOS PWAはバックグラウンドJS停止＋Geofencing API提案放棄でGPS長時間収録が不成立」④「43,200点規模ならCanvas 2D素朴実装（毎フレーム全描画）でrender p95 0.2ms — WebGL不要の規模感較正」（SPIKE-01実測）
- **knowledge/research候補（賞味期限つき）**: ⑤無料PaaS/DB無料枠の2026-07時点比較（Render=Web Service無料継続/Postgres30日失効・Fly.io=新規無料枠廃止・Railway=実質トライアル・Neon=0.5GB恒久・Supabase=500MB/1週pause）⑥OSM/MapTilerタイル利用ポリシー要点（UA必須・非商用限定・ロゴ必須・遮断リスク）

## 6. Definition of Done

- [x] 全RQが decided（12件中10件decided、RQ-09/10は「方向decided・詳細は実装中」— いずれも優先度「実装中可」の範囲内で保留理由を§3.4に明記）
- [x] 各決定に独立2ソース以上の根拠がある（例外はRQ-10のみ=一次1件。方向決定に留め、優先度「実装中可」で許容。§3.4に明記）
- [x] 「今回作る/将来作る/作らない」の3分類がある（§3.3）
- [x] リンク集になっていない（生の8フィールド行はresearch/*.mdに充足。本ファイルは統合判断）
- [x] ARCH.md の選定表・ADRへの反映先が書いてある（§3.4右列）
- [x] SPIKE-01 の結果（PASS/FAIL＋計測値）が §4 に記録されている（PC全PASS。スマホ実機は未計測=ARCH.md §7の残リスクとして引き継ぎ）
